package com.nhlstenden.Viewer;

/**
 * events which can be called in a Mediator to manipulate the Presentation instance
 */
public enum Events {
    OPEN, NEXT_SLIDE, PREV_SLIDE, SAVE, GOTO, NEW, EXIT, CREATE
}
